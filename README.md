## SDL3

Download and build the static libs for SDL

Change the makefile vars, SDL3INCLUDE SDL3LIB SDLSTATICNAME to whatever your path is


## Building
Change the compiler if you need in the makefile
Run `make main`